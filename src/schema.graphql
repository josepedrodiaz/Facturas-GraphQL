type Query {
  info: String!
  feed: [Factura!]!
  getFacturaByIndex(indexId: Int!): Factura!
}

type Mutation {
    post(periodo: String!, monto: Float!): Factura!
  }

type Factura {
  id: ID!
  periodo: String!
  monto: Float!
}
